//Get email lists with different distributions of duplicates
//Get all dedupe methods
//For each email list
	//For each dedupe method
		//Make sure the output is correct
		//Make sure the method returns in less than one second